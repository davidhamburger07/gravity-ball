# Dependencies
node_modules/

# Build output
dist/
dist.zip
build/

# Screenshot output (generated locally for visual QA)
screenshots/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs & temp
*.log
.cache/

# AI playtest run reports. The kept LEVELS in generated/ are deliberately tracked — they are
# the output worth keeping — but a timestamped report per run would just be noise.
generated/report-*.json
generated/unsolved/

# Safety copy the studio writes before appending a generated level to the campaign.
src/data/levels.json.bak

# Stray scratch files
3.11

# Secrets. Upstash REST credentials live here for local dev; never committed.
.env
.env.*
!.env.example
